- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/.nvim:
      relink: true
    ~/.zshrc:
      force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
